export const styles = theme => {
    return ({
        tableWrapper: {
            display: "flex",
            padding: theme.spacing(3),
            overflow: 'auto',
            flexFlow: "column wrap",
            height: "100%",
            flexWrap: "nowrap"
          }
    })
}
